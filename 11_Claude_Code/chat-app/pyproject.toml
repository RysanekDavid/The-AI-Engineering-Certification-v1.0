[project]
name = "chat-app"
version = "0.1.0"
description = "Chat web app skeleton — FastAPI backend with swappable agent seam"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.34",
    "python-dotenv>=1.0",
    "claude-agent-sdk>=0.2.116",
]
